const DOMAIN = 'http://api.w50f.cn'

const BASE_URL = DOMAIN + '/api'

const SUCCESS_CODE = 1

const ERROR_CODE = 0

const ERR_AUTH_CODE = -1

export default {
  DOMAIN,
  BASE_URL,
  SUCCESS_CODE,
  ERROR_CODE,
  ERR_AUTH_CODE
}
